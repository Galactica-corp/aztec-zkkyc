# Public backend base URL for direct browser requests. Leave empty to use relative
# /api requests so a dev proxy or reverse proxy can forward them.
VITE_BACKEND_URL=""
# Server-only proxy target used by the Vite dev server.
BACKEND_PROXY_TARGET="http://localhost:3005"
VITE_PORT="5173"
